import React from 'react';

type ButtonProps = {
  text: string;
  bgColor?: 'bg-purple-700 dark:bg-purple-500';
  outline?: false;
};

/** Button
 * TODO: simplify code, this approach is a hasle and since separating button and link, the complexity is unneeded
 * TODO: Variants: solid, outline
 * TODO: add event Listener prop
 */

export default function Button() {}
